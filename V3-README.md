# Ecrin App Factory V3

V3 introduces real feature recipes.

First recipe: **Image → PDF**.

The generated APK really selects multiple images, creates a multi-page PDF locally, and saves it through Android's system file picker. No broad storage permission is requested.

Unsupported requests fail instead of returning a fake placeholder app.

V0/V1/V2 remain untouched.
