# Ecrin App Factory V4 Agentic

This overlay replaces the recipe-first workflow with a real Codex + AAS Core agentic build.

Workflow: GitHub Actions manual input -> AAS Core 16.4.0 MCP -> Codex -> AAS skill selection -> `aas-stack.json` -> product/UI/ASO analysis -> Android implementation -> APK + AAB artifact.

Required repository secret: `OPENAI_API_KEY`.

Run the workflow named **Ecrin App Factory V4 Agentic** and describe the Android app in natural language.
